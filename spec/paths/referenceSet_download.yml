get:
  tags:
    - Reference sets
  summary: Download one reference set file
  operationId: downloadReferenceSet
  security:
    - bearerAuth: [user]
  parameters:
    - $ref: "../parameters/ID.yml"
  responses:
    "200":
      $ref: "../responses/FileResponse.yml"
