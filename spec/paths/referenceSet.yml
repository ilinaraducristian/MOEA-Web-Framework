get:
  tags:
    - Reference sets
  summary: Get one reference set
  operationId: getReferenceSet
  security:
    - bearerAuth: [user]
  parameters:
    - $ref: "../parameters/ID.yml"
  responses:
    "200":
      description: returns the reference set
      content:
        $ref: "../contents/CommonStructure.yml"
delete:
  tags:
    - Reference sets
  summary: Remove a reference set
  operationId: removeReferenceSet
  security:
    - bearerAuth: [user]
  parameters:
    - $ref: "../parameters/ID.yml"
  responses:
    "200":
      description: reference set removed
