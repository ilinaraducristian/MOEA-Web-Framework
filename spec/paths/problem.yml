get:
  tags:
    - Problems
  summary: Get one problem
  operationId: getProblem
  security:
    - bearerAuth: [user]
  parameters:
    - $ref: "../parameters/ID.yml"
  responses:
    "200":
      $ref: "../responses/CommonStructure.yml"
delete:
  tags:
    - Problems
  summary: Remove a problem
  operationId: removeProblem
  security:
    - bearerAuth: [user]
  parameters:
    - $ref: "../parameters/ID.yml"
  responses:
    "200":
      description: problem removed
