get:
  tags:
    - Algorithms
  summary: Download one algorithm file
  operationId: downloadAlgorithm
  security:
    - bearerAuth: [user]
  parameters:
    - $ref: "../parameters/ID.yml"
  responses:
    "200":
      $ref: "../responses/FileResponse.yml"
